Short: Launch requests approve themselves

When an agent asks to start another task, the approval dialog now starts a countdown and approves itself after five minutes, so a user who stepped away delays the launch instead of blocking it. Declining still cancels it, the completion dialog is deliberately untouched, and Settings → Tasks → "Auto-approve agent launch requests" changes the delay or turns it off.
