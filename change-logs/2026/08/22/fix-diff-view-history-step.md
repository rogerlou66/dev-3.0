Short: Back leaves the diff, not the task

Opening a task's diff is now a real navigation step: Back returns to the task you opened it from and Forward returns to the diff, instead of Back jumping straight out to the board. Applies to every Back gesture — the header arrows, Cmd+[ / Cmd+], the mouse side buttons, the menu, the command palette and Android back.
