CI no longer fails a whole job when the registry serves a bad tarball: all four `bun install` steps in the build workflow now run through a composite action that retries up to three times, wiping `node_modules` and bun's cache between attempts. Every retry is announced as a workflow warning and on the run summary, so the registry's failure rate stays visible instead of being absorbed, and an install that fails all three attempts still fails the job.
