Fixed the fixture race behind the cli-socket flake: a vitest test that fails by timeout keeps executing, and every test in socket-client.test.ts shared one unix socket path, so the zombie could rebind it under its successor and kill it with a real assertion failure. Fixture paths are now derived per test via test-scoped-path.ts, and the one test with a deferred timer cancels it from the test signal.

Also flushed the streamer-mode toggle through act() in the Cmd+2 sensitive-project test, which was letting the keystroke race the guard's React state and evict the whole project screen; gave shift-keys-e2e a fresh FIFO pair and capture file per run; and made the git test spawn mock name the errno when a fork fails, because it resolves to exit code 1 and otherwise reads as a git regression.
