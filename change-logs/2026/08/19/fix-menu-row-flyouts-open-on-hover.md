Short: Hover opens memory and tmux panels

The header kebab's "Memory left" and "tmux Sessions" rows open their panels on hover again, and the panel now appears beside the menu instead of over it, so the pointer can keep moving down the list. A click still pins the panel open, and clicking inside it no longer closes the menu underneath.
