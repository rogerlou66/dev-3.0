The tmux pane controls (split, new window, zoom, layout) and the git action buttons (Diff, Commit, Rebase, Push, Create PR, Merge) are neutral grey instead of blue, finishing the quieting of the inspector session bar. Colour in those rows now belongs only to the status badges — ahead/behind, the PR chip, conflicts — and to Close pane, which turned amber to match Hibernate: both take a session away without touching the work in the worktree.
