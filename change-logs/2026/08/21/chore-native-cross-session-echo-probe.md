Settled the intermittent "bravo journal holds only bravo's output" failure of the native-session lifecycle E2E: the registry never leaked, the second session's own PowerShell drew the first session's command as PSReadLine ghost text out of the user-wide history file, and the journal recorded that faithfully. The fixture now silences the Windows line editor before any marker exists, prints the bytes around a foreign marker so the two possible channels are told apart, and ships a dispatch-only probe that reproduces the crossing with dev3 absent. Every independent proof step in the packaged-runtime job now runs with `if: always()`, so one failure no longer hides the fourteen verdicts after it.
