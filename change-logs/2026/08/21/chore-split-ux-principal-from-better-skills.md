Reconciled the vendored `ux-principal` skill with the `better-*` skill family: it is now mandatory only for changes that add a destination, surface, or action, while craft work on an existing control goes to the owning `better-*` skill. Deleted its duplicated colour/token reference and accessibility sub-agent brief, rewrote the UX bible's quality-floor section down to dev3's own deltas and documented exceptions, and settled the three rules where the two sides contradicted each other — borders may no longer fake depth, `better-ui`'s motion primitives win, and the looping hover animations on the terminal/git/header icon families stay by design. `docs/ux` now has a size budget enforced by a test instead of a prose cap that had drifted from 131 KB to 307 KB. The UX decision log folded to an index — the 24 entries whose reasoning already lives in a decision record became pointers — and both manifest files now state that they are hand-authored rather than generated, after the first pass in this task nearly deleted the yaml on the strength of its own misleading `derived_from_repository` header.
