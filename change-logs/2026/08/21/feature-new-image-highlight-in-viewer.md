Short: See which shared images are new

The shared-images viewer now opens on the FIRST image of the batch an agent just published instead of the last one, rings every new image in the thumbnail strip, and states how many are new in the header. The highlight survives for as long as the viewer stays open, so a batch of three is readable in order.
