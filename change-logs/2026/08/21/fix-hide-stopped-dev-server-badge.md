Short: Kanban cards drop the dev-off badge

The dev-server control on a kanban task card no longer shows an inert "dev off" pill when no server is running — it appears only while the server is starting, running, or its assigned port is squatted by another process.
