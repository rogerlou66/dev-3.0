Recorded the design for running Claude Code and Codex CLI against arbitrary LLM providers through a dev3-managed local proxy sidecar, splitting it into an app-wide model catalog (providers, credentials, named models) and per-preset model roles that bind those names to each CLI's own role slots. Adds a Model routing glossary to AGENTS.md and a decision record covering binary bundling, credential handling, preflight and the known OpenRouter tool-streaming caveat.
