Short: Honest task-sound diagnostics

The task-sound debug probe no longer reports a healthy pipeline while playing nothing, and the audio unlock handlers stay installed so a later click can recover a sound the browser refused. The chimes themselves were still silent on packaged builds after this; that is fixed separately.
