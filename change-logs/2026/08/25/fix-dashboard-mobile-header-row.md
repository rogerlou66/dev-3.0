Short: Dashboard header fits on phones

On a phone the dashboard's project count, New space and Add project no longer share one cramped row — the count sits on its own line and the two buttons split the width below it, so Add project is never pushed off the right edge. The space filter and the search field now match heights on mobile too.
