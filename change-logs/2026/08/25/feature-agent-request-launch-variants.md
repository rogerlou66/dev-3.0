Short: Launch variants from an agent request

When an agent asks to start a task, the approval dialog now lets you add variants before you launch — the same picker the manual Launch modal has. The default is still exactly one task, and the button says how many it will start. The requesting agent is handed one address per variant, because a live variant group shares its seq.
