The backend test suite stops building a real git repository for every diff test: `getTaskDiff`'s recent-mode suite now replays a recorded transcript of real git output instead of spawning git, and the shared git fixture builds its template once per run instead of once per test file and repoints the clone's remote by editing `.git/config` rather than spawning `git remote set-url`. One settings test also pastes the 649-character built-in review prompt instead of typing it keystroke by keystroke. Counted over one backend run, real process spawns drop from 341 to 199 and no test lost an assertion; the suite's 727 real temp directories are spread across some fifty unrelated files and are barely touched here.
