<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
	<!-- Dynamic private LAN hosts cannot be enumerated here; ConnectionUrl rejects public HTTP hosts. -->
	<base-config cleartextTrafficPermitted="true" />
</network-security-config>
